<section class='tab multiattack scrollable {{tab.cssClass}}' data-group='primary' data-tab='multiattack'>
    <div class="settings-list">
        {{#each this.settings.multiattack}}
        <div class="form-group">
            {{#with this}}
            {{#if this.client}}
            {{> mat.setting}}
            {{/if}}
            {{/with}}
        </div>
        {{/each}}
    </div>
</section>